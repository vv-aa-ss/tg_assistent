# TG Assistant Bot

Telegram-бот для управления картами, пользователями, реквизитами и записи данных в Google Sheets.

## Быстрый старт

1. Установите зависимости:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта и укажите переменные:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=123456789,987654321
ADMIN_USERNAMES=admin1,admin2
DATABASE_PATH=./data/bot.db
GOOGLE_SHEET_ID=ваш_id_таблицы
GOOGLE_CREDENTIALS_PATH=./путь/к/credentials.json
```

3. Запуск бота:

```bash
python -m app.main
```

## Основной функционал

### Админ-панель

Бот отвечает только администраторам. При команде `/start` или `/admin` открывается главное меню с разделами:

- **💵 Наличные** - управление наличными и их привязкой к столбцам Google Sheets
- **📇 Безнал** - управление картами, группами карт и реквизитами
- **👥 Пользователи** - управление пользователями и их привязками к картам
- **₿ Крипта** - управление криптовалютами и их привязкой к столбцам Google Sheets
- **📊 Статистика** - просмотр статистики

### Управление картами (Безнал)

- Создание и редактирование карт
- Группировка карт по группам
- Управление реквизитами:
  - Основные реквизиты (хранятся в таблице `cards`)
  - Дополнительные реквизиты (хранятся в таблице `card_requisites`)
  - Добавление, редактирование и удаление реквизитов
  - Привязка столбцов Google Sheets к картам

### Управление пользователями

- Просмотр списка пользователей
- Привязка пользователей к картам
- Просмотр карт пользователя

### Управление криптовалютами

- Добавление криптовалют (BTC, LTC, USDT, XMR-1, XMR-2, XMR-3)
- Привязка криптовалют к столбцам Google Sheets
- Удаление криптовалют

### Управление наличными

- Добавление названий наличных
- Привязка наличных к столбцам Google Sheets
- Удаление наличных

### Получение реквизитов

Если администратор пересылает сообщение пользователя боту:
- Если пользователь привязан к карте, бот отправляет все реквизиты этой карты (основные и дополнительные) отдельными сообщениями
- Если пользователь не привязан, бот предлагает привязать карту

## Команды

### `/admin`
Открывает админ-панель с главным меню.

### `/add`
Добавление данных в Google Sheets в одну строку.

**Процесс:**
1. Выбор типа данных: криптовалюта, карта, наличные
2. Ввод данных (сумма, выбор карты и т.д.)
3. Кнопка "➕" для добавления дополнительных блоков данных
4. Подтверждение записи

**Результат:** Все данные записываются в одну строку таблицы. После записи выводится отчет с датой, записанными данными и адресами ячеек.

### `/rate`
Добавление данных в Google Sheets в режиме rate.

**Отличие от `/add`:**
- Каждая запись идет в первую пустую ячейку соответствующего столбца
- Данные записываются со знаком минус
- Начинается со строки 348 (настраивается в БД)

**Процесс:** Аналогичен `/add`, но данные распределяются по столбцам.

**Результат:** После записи выводится отчет с датой, записанными данными и адресами ячеек.

### `/del`
Удаление последней добавленной строки из Google Sheets.

**Процесс:**
1. Первое подтверждение с кнопками "Да" / "Нет"
2. Второе подтверждение с кнопками "Да" / "Нет"
3. Удаление строки

**Безопасность:** Двойное подтверждение предотвращает случайное удаление.

## Интеграция с Google Sheets

Бот интегрирован с Google Sheets API для записи данных:

- **Режим `/add`**: Все данные записываются в одну строку
- **Режим `/rate`**: Каждая запись идет в первую пустую ячейку столбца
- Автоматическое определение свободных строк
- Привязка карт, криптовалют и наличных к столбцам таблицы

### Настройка Google Sheets

1. Создайте проект в Google Cloud Console
2. Включите Google Sheets API
3. Создайте сервисный аккаунт и скачайте credentials.json
4. Укажите путь к файлу в `.env`: `GOOGLE_CREDENTIALS_PATH`
5. Укажите ID таблицы в `.env`: `GOOGLE_SHEET_ID`
6. Предоставьте доступ к таблице email-адресу сервисного аккаунта

## База данных

Используется SQLite для хранения:
- Пользователей
- Карт и групп карт
- Реквизитов карт
- Привязок пользователей к картам
- Настроек криптовалют и наличных
- Настроек Google Sheets

## Хранение администраторов

- **ID администраторов** указываются в `.env` в переменной `ADMIN_IDS` (через запятую)
- **Username администраторов** указываются в `.env` в переменной `ADMIN_USERNAMES` (через запятую, опционально)
- Файл `.env` не должен коммититься в репозиторий (добавлен в `.gitignore`)

## Структура проекта

```
tg_assistent/
├── app/
│   ├── admin.py          # Обработчики админ-панели и команд
│   ├── config.py         # Конфигурация и настройки
│   ├── db.py             # Работа с базой данных
│   ├── di.py             # Dependency Injection
│   ├── google_sheets.py  # Интеграция с Google Sheets
│   ├── keyboards.py      # Клавиатуры для бота
│   └── main.py           # Точка входа
├── data/
│   └── bot.db            # База данных SQLite
├── logs/
│   └── bot.log           # Логи бота
├── .env                  # Переменные окружения (не коммитится)
├── requirements.txt      # Зависимости проекта
└── README.md            # Документация
```

## Логирование

Все события бота логируются в файл `logs/bot.log` с уровнем DEBUG для детального отслеживания работы.

## Технологии

- Python 3.12+
- aiogram 3.x (Telegram Bot API)
- SQLite (база данных)
- Google Sheets API
- asyncio (асинхронное программирование)
